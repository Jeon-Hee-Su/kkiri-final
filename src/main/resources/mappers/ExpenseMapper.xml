<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kkiri.mapper.ExpenseMapper">

    
<insert id="insertExpense" parameterType="com.kkiri.model.dto.ExpenseRequest">
    <selectKey keyProperty="expenseId" resultType="int" order="BEFORE">
        SELECT EXPENSES_SEQ.NEXTVAL FROM DUAL
    </selectKey>
    INSERT INTO EXPENSES (
        expense_id, 
        group_id, 
        paid_by, 
        amount, 
        merchant_name, 
        category,
        payment_account_type, 
        status
    ) VALUES (
        #{expenseId}, 
        #{groupId}, 
        #{paidBy}, 
        #{amount}, 
        #{merchantName}, 
        #{category},
        #{paymentAccountType}, 
        'ACTIVE'
    )
</insert>

    <insert id="insertReceiptItems" parameterType="com.kkiri.model.dto.ExpenseRequest">
    DECLARE
    BEGIN
        <foreach collection="items" item="item" separator=";">
            INSERT INTO RECEIPT_ITEMS (
                item_id, 
                expense_id, 
                item_name, 
                price, 
                quantity
            ) VALUES (
                RECEIPT_ITEMS_SEQ.NEXTVAL, 
                #{expenseId}, #{item.itemName}, 
                #{item.price}, 
                #{item.quantity}
            )
        </foreach>
        ; -- PL/SQL 문장의 마지막 세미콜론
    END;
</insert>

</mapper>